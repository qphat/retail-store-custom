apiVersion: v1
kind: Service
metadata:
  name: catalog
spec:
  selector:
    app: catalog       # matches pods with label app=catalog
  ports:
    - port: 80         # port exposed inside the cluster
      targetPort: 8080 # port the container listens on
